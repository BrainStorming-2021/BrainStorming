from collections import deque

stack = []

def solution (num):
        
        calculator= deque([seq for seq in range(1, num+1)])
            recul(calculator)
                
                    return stack

                # recursive
                def recul(arr):
                        if arr :
                                    stack.append(arr.popleft())
                                            if arr :
                                                            arr.append(arr.popleft())
                                                                    else :
                                                                                    return stack
                                                                                    return recul(arr)
                                                                                    
                                                                                print(solution(7))
                                                                                complcache_start_auto_complete)OB
